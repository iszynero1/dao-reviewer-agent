
modules = ["python-3.12"]

[nix]
channel = "stable-25_05"
packages = ["libxcrypt"]

[deployment]
run = ["sh", "-c", "uvicorn main:app --host 0.0.0.0 --port 5000"]

[workflows]
runButton = "Run DAO Agent"

[[workflows.workflow]]
name = "Run DAO Agent"
author = 45817434
mode = "sequential"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "uvicorn main:app --host 0.0.0.0 --port 5000"

[[ports]]
localPort = 5000
